# Mobile (Flutter)

Project Flutter đã được init sẵn trong thư mục `mobile`.

## Setup
```bash
cd /Users/admin/Desktop/run-with-codeX/mobile
flutter pub get
```

## Run
```bash
flutter run
```

## Mục tiêu tích hợp
- Dùng chung API backend:
  - Auth: login/register/refresh/profile
  - Products
  - Orders
  - Livestream room APIs
- Dùng Socket.IO namespace `/livestream` cho chat và signaling.

# product-v1-mobile
